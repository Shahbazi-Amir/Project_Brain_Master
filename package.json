{
  "name": "project-brain-master",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first project operating system that supervises Codex-powered execution loops.",
  "engines": {"node": ">=24.12.0 <25"},
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "ci": "npm run typecheck && npm test"
  },
  "devDependencies": {"@types/node": "^24.0.0", "typescript": "^5.9.0"}
}
