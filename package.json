{
  "name": "project-brain-master",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Local-first project operating system that frames ideas, confirms adaptive choices, builds execution contracts, supervises and reviews Codex-powered execution.",
  "engines": {"node": ">=22.18.0 <23 || >=24.12.0 <25"},
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check:ui": "node --check public/app.js",
    "ci": "npm run typecheck && npm run check:ui && npm test"
  },
  "devDependencies": {"@types/node": "^22.18.0", "typescript": "^5.9.0"}
}
